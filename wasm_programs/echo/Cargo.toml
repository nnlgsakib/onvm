[package]
name = "onvm_echo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spin = { version = "0.9", features = ["mutex", "once"] }
wee_alloc = "0.4.5"

[profile.release]
lto = true
opt-level = "z"
panic = "abort"
strip = true
codegen-units = 1
